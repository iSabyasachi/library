spring.application.name=library

# Expose a few actuator endpoints for the "latest Spring Boot" tour.
management.endpoints.web.exposure.include=health,info,mappings,beans
management.endpoint.health.show-details=always

# Info shown at /actuator/info
info.app.name=Java + Kotlin interop demo
info.app.spring-boot=4.1.0
